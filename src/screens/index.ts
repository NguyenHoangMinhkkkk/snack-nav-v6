import { ConfigsType } from '../navigationType';
import { ScreenParamsType } from './type';

import ProfileScreen from './ProfileScreen';

export default {
  profileScreen: {
    component: ProfileScreen,
  },
} as ConfigsType<ScreenParamsType>;
